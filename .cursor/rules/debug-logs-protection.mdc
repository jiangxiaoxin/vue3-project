---
description: 禁止擅自删除或改动已有调试日志；相关操作必须先询问用户
alwaysApply: true
---

# 调试日志保护规则

## 强制要求

对代码中**已有的调试日志**（例如 `console.log` / `console.debug` / `console.warn` / `console.error` / `debugger`，以及明显用于临时排查的日志语句）：

1. **绝对禁止**在未获得用户明确允许的情况下删除。
2. **绝对禁止**在未获得用户明确允许的情况下静默注释、改写语义或挪走这些日志。
3. 若你的改动会触及这些日志（重构、格式化、合并函数、提取模块、清理代码等），**必须先向用户提问**，说明：
   - 你打算做什么
   - 会影响到哪些调试日志（文件路径 + 日志原文或附近代码）
4. 等待用户给出处理方式后再继续；用户未回复前，**不得**自行决定删除或改掉这些日志。

## 允许的做法

- 在**不改动已有调试日志**的前提下，可以新增必要的调试日志。
- 仅在用户明确指示（例如“删掉这行 log”“改成 xxx”）后，才可按指示处理对应日志。

## 反例

```ts
// ❌ 未询问用户就删除
// console.log('delta::', delta)

// ❌ 未询问用户就“顺手清理”
// console.log("steam is done000000000000000000")
```

```ts
// ✅ 保留已有日志，需要时先问用户
console.log('delta::', delta)
reactiveAssistantMessage.content += delta
```
