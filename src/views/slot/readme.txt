1.父组件可以通过slot往子组件内插入代码段
2.插入的代码段里，可以访问父组件作用域下的变量和方法。因为本质就是父组件范围内的代码。
3.子组件里可以写多个slot，这样就需要子组件给slot确定个名字，父组件通过具名slot来决定代码段添加到哪里
4.父组件传入子组件的代码段，默认下是无法访问子组件内的数据的。但可以通过作用域插槽：将子组件内的数据跟其他变量一样的写法，绑到slot上。
然后在父组件内，写这些代码段时，用v-slot绑一个本地变量名，那这段代码里就可以通过这个变量名，访问到子组件内的数据了。